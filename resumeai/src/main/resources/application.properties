# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/resume_ai
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.open-in-view=false

server.port=8080

# JWT Configuration
jwt.secret=8f3c6a1d4b7e9f2c5a8d1e4f7b9c2d5e8a1f4c7b9d2e5f8a1c4d7e9b2f5a8c1
jwt.access-token-expiration=900000
jwt.refresh-token-expiration=604800000

# Google Authentication
google.client-id=${GOOGLE_CLIENT_ID}

management.endpoints.web.exposure.include=health,info

# Multipart configuration for File Uploads
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# AWS S3 Configuration
aws.s3.bucket-name=resumeai-uploads-aryan
aws.region=ap-south-1
aws.access-key=${AWS_ACCESS_KEY}
aws.secret-key=${AWS_SECRET_KEY}